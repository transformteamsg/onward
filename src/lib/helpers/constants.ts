export const HOME_PATH = '/home';

/**
 * The minimum score, as a percentage, that a learner must reach to pass a required unit's quiz.
 * The server owns the verdict — the client uses this value for display only.
 */
export const PASSING_SCORE = 80;
